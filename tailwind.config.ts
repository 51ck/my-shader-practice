import type { Config } from "tailwindcss";

const Configuration: Config = {
  content: [
    './index.html',
    './src/**/*.vue',
  ],

  theme: {
    extend: {},
  },

  plugins: [],
};

export default Configuration
